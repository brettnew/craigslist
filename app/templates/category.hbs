<h2>{{model.name}}</h2>

<br>

<h4>Current listings:</h4>

{{#each model.listings as |listing|}}
  <ul>
    <li>{{#link-to 'listing' listing.id}} {{listing.title}} {{/link-to}}</li>
  </ul>
{{/each}}
